import DosingTable from '../../shared/DosingTable'
import { ACTIVE_COLOR } from '../../../data/active'

export default function Slide06Dosing() {
  return <DosingTable branchColor={ACTIVE_COLOR} />
}
